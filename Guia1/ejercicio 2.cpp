#include <stdio.h>
#include <stdlib.h>
#include <math.h>



int main () {
int x,y,a,b;
float formulaA;
float formulaB;
float formulaC;
float formulaD;
float formulaE;
printf ("escriba valor de x:");
scanf ("%d",&x);
printf ("escriba valor de y:");
scanf ("%d",&y);
formulaA = (x+y)/(x-y);
//formulaB = pow(pow((a+b),2),(a+b)); //´pow (numero,potencia);

formulaB = ((a+b)*(a+b))*((a+b)*(a+b));
printf ("%f",formulaB);













return 0;
}
